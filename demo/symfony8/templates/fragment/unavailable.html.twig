<div class="fragment-fallback" role="status">
    Fragment unavailable
    {%- if status_code is defined and status_code -%}
        (HTTP {{ status_code }})
    {%- endif -%}.
    Parent page continued successfully.
</div>
